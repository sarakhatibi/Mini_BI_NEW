# AI_USAGE

## از چه ابزارهای AI استفاده شد؟
- **Devin (Cognition)** برای بازبینی کد موجود، دیباگ خطاهای تحلیل داده روی دیتاست واقعی،
  بازنویسی لایه سرویس‌ها و نوشتن تست‌ها.

- **ChatGPT / مدل‌های زبانی** برای ساخت اسکلت اولیه اپ Streamlit، توابع خواندن فایل و
  نسخه اول KPI و نمودارها.
- **GitHub Copilot:** برای تکمیل خودکار کد، نوشتن توابع تکراری و سریع‌نویسی استفاده شد. بیشتر در سطح کدنویسی روزمره کمک کرد.

- **DeepSeek:** برای بحث‌های معماری، بررسی سناریوهای مختلف و رفع مشکلات پیچیده‌تر. DeepSeek بیشتر نقش هم‌فکری رو داشت تا کدنویسی.


## در چه قسمت‌هایی استفاده شدند؟

| بخش | نقش AI | نقش من |
|---|---|---|
| رابط Streamlit | تولید اسکلت اولیه صفحه و ویجت‌ها | چیدمان تب‌ها، مسیر Upload → Analysis → Decision |
| خواندن فایل | پیشنهاد `read_csv`/`read_excel` | افزودن تشخیص encoding و جداکننده و انتخاب شیت |
| پاک‌سازی داده | پیاده‌سازی توابع normalize و parse | تعریف قواعد کسب‌وکار: چه چیزی اصلاح شود و چه چیزی فقط گزارش |
| پروفایل معنایی | نوشتن کد تشخیص نقش ستون | تصمیم معماری «همه‌چیز از روی پروفایل، نه نام ثابت ستون» |
| KPI و بینش | تولید فرمول‌ها و متن‌ها | انتخاب شاخص‌های مدیریتی و تعریف فروش خالص/مطالبات |
| تست‌ها | تولید تست‌ها بر اساس سناریوهای من | انتخاب مسیرهای Critical |

## پیشنهادهای اشتباه AI که اصلاح شد

1. **تشخیص ستون عددی فقط از روی dtype.** چون `Total_Amount_USD` و `Unit_Price_USD` به‌صورت
   متن ذخیره شده بودند، ستون مبلغ اصلاً «عددی» دیده نمی‌شد و KPI فروش و نمودارها به‌جای مبلغ
   سراغ `Quantity` می‌رفتند. با `numeric_utils.parse_number` و پاک‌سازی قبل از تحلیل حل شد.
2. **تبدیل هر رشته شبیه عدد به عدد.** نسخه پیشنهادی، `ORD-14040001` و `P-104` را به float
   تبدیل می‌کرد و شناسه‌ها را نابود می‌کرد. قاعده اضافه شد: فقط واحد/نماد ارز مجاز اطراف عدد
   پذیرفته می‌شود، نه پیشوند حروفی دلخواه.
3. **نادیده گرفتن تاریخ شمسی.** `1405/02/31` به‌عنوان «مقدار خراب» حذف می‌شد؛ به‌جای آن
   مبدل جلالی→میلادی نوشته شد.
4. **حذف رکوردهای تکراری شناسه.** پیشنهاد AI حذف همه ردیف‌های با `Order_ID` تکراری بود؛
   این کار داده واقعی را از بین می‌برد، پس فقط ردیف‌های *کاملاً* تکراری حذف می‌شوند و شناسه
   تکراری صرفاً در تب کیفیت گزارش می‌شود.
5. **KPIهای Hard Code.** نسخه اول لیست ثابتی از نام ستون‌ها (`Total_Amount_USD`, `Quantity`, ...)
   را جستجو می‌کرد و روی هر دیتاست دیگری بی‌خروجی بود؛ جای آن `semantic_profiler` نشست.
6. **رشد ماهانه گمراه‌کننده.** مقایسه ماه ناقص جاری با ماه قبل، افت ۹۳٪ نشان می‌داد؛ حالا ماه
   ناتمام از محاسبه کنار گذاشته و این موضوع به کاربر اعلام می‌شود.
7. **نویز در گزارش کیفیت.** ستون متنی `Payment_Term` («۳۰ روزه»، «نقدی») به‌عنوان «عدد خراب»
   علامت می‌خورد و ۱۲٪ رکوردها «مقدار پرت» گزارش می‌شد؛ آستانه‌ها اصلاح شدند.



## تصمیمات معماری که خودم گرفتم

### نگاه کلی به قضیه

وقتی شروع کردم به طراحی این سیستم، سه تا اصل اساسی رو توی ذهنم داشتم که همه تصمیمات بعدی بر اساس اونها شکل گرفت:

**اول**: کاربر نهایی یه مدیر غیرفنیه. پس نباید هیچ جا مجبور باشه تنظیمات فنی انجام بده یا چیزی رو کانفیگ کنه. هر تصمیمی که می‌گرفتم، خودم رو جای اون مدیر می‌ذاشتم و می‌گفتم "آیا این کار رو برایش ساده‌تر می‌کنه؟"

**دوم**: سیستم نباید به یه دیتاست خاص وابسته باشه. این یعنی هیچ اسم ستونی نباید Hard Code بشه. سیستم باید خودش تشخیص بده هر ستون چه نقشی داره.

**سوم**: کاربر باید متوجه بشه چه اتفاقی برای داده‌هاش افتاده. هیچ تغییری نباید پنهان بمونه. این باعث شد کل فرآیند پاکسازی رو طوری طراحی کنم که قابل ردیابی باشه.

---

### ۱. لایه‌ها رو از هم جدا کردم

**تصمیم**: لایه سرویس رو کامل از UI جدا کردم. `app.py` فقط کارش نمایشه و هیچ منطق تحلیلی توش نیست.

**چرا؟** خب، چند دلیل داشت:
- هر بخش رو میشه مستقل تست کرد (خیلی کمک کرد موقع دیباگ)
- اگه بخوام UI رو عوض کنم، منطق کسب‌کار دست نمیخوره
- کد تمیزتر و خواناتره
- توی تیم، هرکس میتونه رو یه بخش کار کنه بدون اینکه به بقیه گره بخوره

---

### ۲. یه مسیر مشخص برای پردازش دادم

**تصمیم**: همه چیز از یه نقطه واحد عبور میکنه: `services/pipeline.build_analysis`. ترتیبشم ثابته: `clean → profile → analyze → quality`.

**چرا؟** اینطوری:
- میدونم دقیقاً داده در هر مرحله کجاست و چه شکلیه
- اگه خطایی بیاد، دقیقاً میدونم کدوم مرحله مشکل داره
- اضافه کردن یه مرحله جدید توی آینده خیلی راحته
- نوشتن تست برای کل فرآیند ساده‌تر شده

---

### ۳. پاکسازی شفاف

**تصمیم**: هر تغییری که روی داده اعمال میشه، توی `CleaningResult.actions` ذخیره میشه و به کاربر نشون داده میشه. داده خام هم دست نخورده میمونه.

**چرا؟** این یه تصمیم کلیدی بود:
- کاربر باید بدونه داده‌اش چطور تغییر کرده (مخصوصاً اگه عددی عوض شده باشه)
- هیچ داده‌ای حذف نمیشه مگر ردیف‌های کاملاً تکراری (حیفه داده از دست بره)
- وقتی کاربر میبینه سیستم شفافه، بهش اعتماد بیشتری پیدا میکنه
- اگه اشتباهی رخ بده، کاربر میتونه تغییرات رو بررسی کنه

---

### ۴. خروجی به فارسی و با توضیح

**تصمیم**: همه خروجی‌ها به فارسی هستن و هر عدد با توضیحش کنار هم میاد.

**چرا؟** مدیر قراره از این خروجی استفاده کنه. یه عدد تنها، فقط یه عدده. اما وقتی توضیحش رو هم بدی، تبدیل میشه به یه بینش کاربردی. مثلاً به جای اینکه بنویسم "فروش کل: ۱۰۰,۰۰۰", مینویسم "فروش کل ۱۰۰,۰۰۰ دلار بوده که نسبت به ماه قبل ۱۵٪ رشد داشته". اینطوری مدیر میدونه الان باید چه کاری انجام بده.

---

### ۵. KPI ها رو شرطی کردم

**تصمیم**: KPIها فقط وقتی ساخته میشن که داده پشتیبان داشته باشن. اگه داده‌ای برای یه شاخص وجود نداشته باشه، اصلاً نشونش نمیدم.

**چرا؟** 
- نمایش یه عدد اشتباه یا صفر، کاربر رو گیج میکنه
- بهتره چیزی نشون داده نشه تا اینکه اطلاعات غلط به کاربر بدیم
- کاربر فقط چیزایی رو میبینه که قابل اعتماد هستن
- این کار اعتماد کاربر رو جلب میکنه چون میدونه هر چی میبینه درسته

---

### یه نگاه کلی به تصمیماتم

همه این تصمیمات به یه هدف ختم میشد: **تبدیل داده خام به تصمیم** برای یه مدیر غیرفنی. هر بار که می‌خواستم تصمیمی بگیرم، از خودم می‌پرسیدم:

> "آیا این کار به مدیر کمک میکنه سریع‌تر و بهتر تصمیم بگیره؟"

اگه جواب مثبت بود، انجامش دادم. اگه نه، روش دیگه‌ای رو امتحان کردم.

در نهایت، سیستمی تحویل دادم که نه تنها کار میکنه، بلکه میشه روش حساب کرد. کاربر میدونه داده‌اش چطور پردازش شده، میدونه هر عدد از کجا اومده، و میتونه به خروجی‌ها اعتماد کنه. این یعنی مدیریت میتونه بدون نگرانی از صحت داده‌ها، تصمیم‌های مهم رو بگیره.





## نمونه Prompt مهم

> «این pipeline نباید به نام ستون‌های این فایل وابسته باشد. یک پروفایل معنایی بساز که با
> ترکیب نام ستون و آمار مقادیر، نقش هر ستون (مبلغ، درصد، تاریخ، شناسه، دسته‌بندی، متن) را
> تشخیص بدهد؛ سپس KPI و نمودار را فقط از روی همین نقش‌ها بساز. شناسه‌هایی مثل ORD-14040001
> نباید به عدد تبدیل شوند و هیچ رکوردی جز ردیف کاملاً تکراری نباید حذف شود — بقیه موارد فقط
> در گزارش کیفیت داده گزارش شوند.»

نمونه دیگر برای دیباگ:

> «روی Test_Dataset.xlsx اجرا کن و به من نشان بده چه ستون‌هایی numeric تشخیص داده شده‌اند و
> چرا فروش خالص با مجموع Total_Amount_USD فرق دارد.»


نمونه دیگه از پزامپتی که زدم :
یک **Modular Architecture** با **Separation of Concerns** و **Pipeline Pattern** طراحی کن.
سیستم باید **Adaptive** و **Context-Aware** باشد و روی هر دیتاستی کار کند.
از **Heuristic** و **Semantic Analysis** برای تشخیص نقش ستون‌ها استفاده کن.
**Non-destructive Cleaning** با **Traceability** کامل پیاده‌سازی کن.
همه **Edge Cases** را با **Robust Error Handling** مدیریت کن.
**Actionable Insights** به فارسی با **Visual Hierarchy** تولید کن.
**Testability** و **Performance Optimization** (با **Vectorized Operations**) را رعایت کن.

Before writing code, explain your approach and ask me clarifying questions.
